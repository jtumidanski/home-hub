---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: calendar-service
  namespace: home-hub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: calendar-service
  template:
    metadata:
      labels:
        app: calendar-service
    spec:
      containers:
      - name: calendar-service
        image: ghcr.io/jtumidanski/home-hub-calendar-service:main
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
        - name: DB_HOST
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_HOST
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_PASSWORD
        - name: DB_NAME
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_NAME
        - name: JWKS_URL
          value: "http://auth-service:8080/api/v1/auth/.well-known/jwks.json"
        - name: GOOGLE_CALENDAR_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: calendar-secrets
              key: GOOGLE_CALENDAR_CLIENT_ID
        - name: GOOGLE_CALENDAR_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: calendar-secrets
              key: GOOGLE_CALENDAR_CLIENT_SECRET
        - name: CALENDAR_TOKEN_ENCRYPTION_KEY
          valueFrom:
            secretKeyRef:
              name: calendar-secrets
              key: CALENDAR_TOKEN_ENCRYPTION_KEY
        - name: SYNC_INTERVAL_MINUTES
          value: "15"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8080
          initialDelaySeconds: 3
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: calendar-service
  namespace: home-hub
spec:
  selector:
    app: calendar-service
  ports:
  - protocol: TCP
    port: 8080
