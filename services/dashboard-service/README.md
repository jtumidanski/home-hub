# dashboard-service

Go microservice that stores and serves per-user dashboard layouts for the Home Hub UI.

To run locally alongside the rest of the stack, use `scripts/local-up.sh` from the repo root; it handles the `.env` file and Docker Compose build context automatically. Domain model and API surface are documented in `docs/domain.md` (to be written in a later phase).
